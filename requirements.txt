# ---- Core runtime ----
pandas>=2.0,<3.0
numpy>=1.24,<3.0
scipy>=1.10
scikit-learn>=1.3
psycopg2-binary>=2.9
sqlalchemy>=2.0
pydantic>=2.0
qdrant-client>=1.7
requests>=2.31
python-dotenv>=1.0
python-json-logger>=2.0

# ---- MetaTrader 5 (Windows ONLY — the package talks to a running terminal) ----
MetaTrader5>=5.0.45; sys_platform == "win32"

# ---- News / sentiment (Phase 6; FinBERT optional, CPU ok) ----
transformers>=4.38
torch>=2.1; sys_platform == "win32"

# ---- Dev / tests ----
pytest>=8.0
